export const metadata = {
  title: "Chicken Nation | Login",
  description: "Connexion",
};

export default function AuthLayout({ children }) {
  return (
    <div>
        {children}
    </div>
  );
}
